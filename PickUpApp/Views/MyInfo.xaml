<?xml version="1.0" encoding="UTF-8"?>
<ContentPage Title="My Info" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="PickUpApp.MyInfo">
<ContentPage.Padding>
    <OnPlatform x:TypeArguments="Thickness">
      <OnPlatform.iOS>0, 20, 0, 0</OnPlatform.iOS>
    </OnPlatform>
  </ContentPage.Padding>
	<ContentPage.Content>
	<StackLayout x:Name="stacker" Orientation="Vertical"><ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" Color="Black" />
	<!---
	<Label Text="Please provide this minimal information so everyone can keep in touch!"></Label>
	<Entry Placeholder="First Name" Text="{Binding Firstname}"></Entry>
	<Entry Placeholder="Last Name" Text="{Binding Lastname}"></Entry>
	<Entry Placeholder="Cell Phone" Keyboard="Telephone" Text="{Binding Phone}"></Entry>
	<Entry Placeholder="Email" Keyboard="Email" Text="{Binding Email}"></Entry>
	-->
	</StackLayout>
	</ContentPage.Content>
</ContentPage>
